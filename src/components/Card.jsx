

function Card(props) {
    return ( 
        <div>
            <h3>{props.title}</h3>
            <p>{props.role}</p>
            <img src={props.img} alt=""/>
        </div>
     );
}

<div>

</div>
export default Card;